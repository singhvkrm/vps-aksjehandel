## notice we use named parameters in the queries, eg :#name. A named query parameter must start with :#
## sql that update bank balance


##sql.updateInfo=update bank set balance=balance - :#sum where clientid = :#clientId

sql.update=UPDATE bank SET balance-:#sum WHERE clientid=:#clientId
